#!/bin/bash
#a=$(pwd)
#sh /$a/env

cd $LFS/sources/diffutils-3.8

./configure --prefix=/usr --host=$LFS_TGT

make -j3

make DESTDIR=$LFS install

make distclean

echo "diffutils_pass_1_finish" >> $LFS/sources/install.log

