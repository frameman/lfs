#!/bin/bash

cd $LFS/sources/make-4.4

sed -e '/ifdef SIGPIPE/,+2 d' \
    -e '/undef  FATAL_SIG/i FATAL_SIG (SIGPIPE);' \
    -i src/main.c

./configure --prefix=/usr

make -j3
#make check
make install

if [ make ]
then
echo "please run make install again if you not sure make is install or not" >> ../make_all/install.log
fi

