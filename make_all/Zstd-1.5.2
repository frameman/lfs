#!/bin/bash
cd ../zstd-1.5.2

patch -Np1 -i ../zstd-1.5.2-upstream_fixes-1.patch
make prefix=/usr
#make check
make prefix=/usr install
rm -v /usr/lib/libzstd.a

if [ /usr/bin/libzstd ]
then
echo "zstd installed" >> ../make_all/install.log
else
echo "zstd not installed" >> ../make_all/install.log
fi
